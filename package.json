{
  "name": "likened-b2b",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npx http-server public -p 8080",
    "serve": "npx kill-port 8080 && npm run build && npx http-server dist -p 8080 -o"
  }
}
